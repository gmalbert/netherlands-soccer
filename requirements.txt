# Keep the public index authoritative for ordinary packages. The deployment
# runtime only needs the consumer-facing prediction app, not the training
# stack (PyTorch, SHAP, or CUDA packages).
--index-url https://pypi.org/simple
pitch-oracle-core[runtime] @ git+https://github.com/gmalbert/pitch-oracle-core.git@eb7750c5fd033213051511723e35ec253c3f1ca8
streamlit==1.60.0
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
xgboost==3.3.0
